import { AppShell } from "@/components/app-shell";
import { clients, seedTasks } from "@/lib/seed-data";

export default function ClientsPage() {
  return <AppShell><main className="mx-auto max-w-7xl p-4 sm:p-8"><div className="flex items-end justify-between"><div><p className="text-sm font-semibold text-steel">CRM</p><h1 className="mt-1 text-3xl font-bold">Clients</h1><p className="mt-1 text-sm text-slate-600">Your active agency accounts and their work in flight.</p></div><button className="rounded-lg bg-navy px-4 py-2.5 text-sm font-semibold text-white">Add client</button></div><section className="card mt-6 overflow-hidden"><div className="overflow-x-auto"><table className="w-full text-left text-sm"><thead className="bg-slate-50 text-xs uppercase tracking-wide text-slate-500"><tr><th className="px-5 py-3">Client</th><th className="px-4 py-3">Type</th><th className="px-4 py-3">Priority</th><th className="px-4 py-3">Open tasks</th><th className="px-4 py-3">Status</th></tr></thead><tbody>{clients.map(name=>{const tasks=seedTasks.filter(task=>task.client===name);const high=tasks.some(task=>task.priority==='High');return <tr key={name} className="border-t hover:bg-slate-50"><td className="px-5 py-4 font-semibold">{name}</td><td className="px-4 py-4 text-slate-600">{name.includes('Seeds')||name==='Nuziveedu'||name==='Kaveri'||name==='Kurnool Seeds'?'Seeds':'Ag-Inputs / Bio'}</td><td className="px-4 py-4"><span className={`pill ${high?'bg-red-50 text-red-700':'bg-slate-100 text-slate-700'}`}>{high?'High':'Medium'}</span></td><td className="px-4 py-4 font-semibold">{tasks.length}</td><td className="px-4 py-4"><span className="pill bg-green-50 text-green-700">Active</span></td></tr>})}</tbody></table></div></section></main></AppShell>;
}
