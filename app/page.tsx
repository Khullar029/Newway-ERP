import { AppShell } from "@/components/app-shell";
import { TaskBoard } from "@/components/task-board";
import { clients, seedTasks } from "@/lib/seed-data";
import { ArrowUpRight, CheckCircle2, Clock3, TriangleAlert } from "lucide-react";
const inProgress = seedTasks.filter(t=>t.status === "In Progress").length;
const highOpen = seedTasks.filter(t=>t.priority === "High" && t.status !== "Done").length;
const kpis = [["Active clients",clients.length,"All agency accounts",CheckCircle2,"text-success bg-green-50"],["Open tasks",seedTasks.length,"Across 8 work lanes",Clock3,"text-steel bg-blue-50"],["High priority",highOpen,"Needs owner attention",TriangleAlert,"text-amber-700 bg-amber-50"],["In progress",inProgress,"Moving toward launch",ArrowUpRight,"text-purple-700 bg-purple-50"]] as const;
export default function Home() { return <AppShell><div className="mx-auto max-w-7xl space-y-6 p-4 sm:p-8"><section className="flex flex-col justify-between gap-3 sm:flex-row sm:items-end"><div><p className="text-sm font-semibold text-steel">OVERVIEW</p><h1 className="mt-1 text-2xl font-bold tracking-tight sm:text-3xl">Good morning, Piyush</h1><p className="mt-1 text-sm text-slate-600">Your agency is running 24 active tasks across 16 clients.</p></div><button className="rounded-lg bg-navy px-4 py-2.5 text-sm font-semibold text-white shadow-sm">View launch sprint</button></section><section className="grid gap-4 sm:grid-cols-2 xl:grid-cols-4">{kpis.map(([title,value,sub,Icon,style])=><article className="card p-5" key={title}><div className="flex justify-between"><div><p className="text-sm font-medium text-slate-600">{title}</p><p className="mt-2 text-3xl font-bold">{value}</p></div><div className={`grid h-10 w-10 place-items-center rounded-lg ${style}`}><Icon size={20}/></div></div><p className="mt-3 text-xs text-slate-500">{sub}</p></article>)}</section><section className="grid gap-6 xl:grid-cols-[1fr_320px]"><div><div className="mb-3 flex items-center justify-between"><div><h2 className="font-bold">Agency task board</h2><p className="text-sm text-slate-500">Update status directly; changes sync live for your team.</p></div></div><TaskBoard/></div><aside className="space-y-4"><div className="card p-5"><p className="text-sm font-bold">Upcoming launch</p><div className="mt-4 rounded-lg border border-blue-100 bg-blue-50 p-4"><p className="text-xs font-bold text-steel">01 SEP 2026</p><p className="mt-1 font-bold text-navy">Nidhi Rabi 2026</p><p className="mt-1 text-xs leading-5 text-slate-600">Cumin · Coriander · Mustard<br/>Gujarat · Rajasthan · MP</p><button className="mt-3 text-sm font-semibold text-steel">Open campaign →</button></div></div><div className="card p-5"><p className="text-sm font-bold">Tasks by lane</p><div className="mt-4 space-y-3">{["Campaigns","Calendar + Strategy","Content Production","Lead-Gen Strategy"].map(lane=>{const count=seedTasks.filter(t=>t.lane===lane).length;return <div key={lane}><div className="flex justify-between text-xs"><span>{lane}</span><span className="font-bold">{count}</span></div><div className="mt-1.5 h-1.5 overflow-hidden rounded bg-slate-100"><div className="h-full rounded bg-steel" style={{width:`${count/10*100}%`}}/></div></div>})}</div></div></aside></section></div></AppShell>; }
